# 2026-07-18 日工作日志

## NS vs BM4 分析

### 完成事项
1. 探索 `Program/expansion_tool.exe`：确认 NS 工具不充分（仅极限 expand），BM4 完整支持
2. BM4 基本列系统枚举：从 (0) 到 (0,0,0)(1,1,1)(2,2,0)，记录所有关键极限表达式的基本列模式
3. 发现 BM4 等价性：(0)(1,1) = (0,0)(1,1) 等，前导 0 行不改变序数
4. BM4 基本列模式分类：线性增长、周期重复、交错模式
5. NS 定义手算：计算 f(α) 和 S(α) 对 0→ω→ω·2→ω² 的值
6. 发现 NS 核心规律：f(ω·k)=4^k, S(n)=1-1/2^(n-1), S(ω)=1, S(ω·k)→S(ω²)=5/3
7. **修正**：NS 是 1-indexed（S(1)=0），之前误用 0-indexed（S(0)=0），所有 S 值已修正减 1
8. 写入 ANALYSIS_TABLE.md、MEMORY.md、README.md（追加）
9. 创建 ns_compute.py 辅助计算脚本
10. 改用加权分数展开表示（每步 1/f 分开显示），创建 ns_fraction.py 和 ns_higher.py
11. 计算 S(ω^k) 通项: S(ω^k) = 1 + (8/9)(1-4^{1-k})
12. S(ω^ω) = 17/9, 之后 f 重置为 16，S 可突破 2
13. Git 初始化和提交
14. **重要修正**: f(ω^k) = 2^{2^k} (双指数!), 非 4^k。S(ω²)=5/3, S(ω³)=11/6, S(ω⁴)≈1.844, S(ω^ω)≈1.844 (几乎停在 ω⁴)
15. f(ω^ω)=16 重置，ω^ω→ω^(ω+1) 贡献 +1/6，S(ω^(ω+1))≈2.010 > 2
8. 创建 ns_compute.py 辅助计算脚本
